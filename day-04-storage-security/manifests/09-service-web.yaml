apiVersion: v1
kind: Service
metadata:
  name: web-service
  labels:
    app: web
    tier: frontend
    component: capstone
spec:
  type: ClusterIP
  selector:
    app: web
    tier: frontend
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
